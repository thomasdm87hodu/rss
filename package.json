{
  "name": "breaking-israel-news-rss",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "setup:webhook": "node --env-file=.env scripts/setup-telegram-webhook.mjs"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "framer-api": "^0.1.29"
  }
}
